# TODO Frontend / API

- Crear un endpoint para listar entrenadores por sucursal y poblar el selector de `trainerUserId` en clases sin usar entrada manual.
- Agregar un endpoint agregado de dashboard con metricas y tendencias para evitar fan-out de consultas desde el frontend.
- Considerar un endpoint de detalle enriquecido para bookings con nombres de cliente y clase ya resueltos desde backend.

